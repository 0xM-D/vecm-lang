const int fibSize = 50;
int[] fibArray = [0, 1].pushMultiple(-1, fibSize)

fib := fn(i: int)->int {
    if(fibArray[i] < 0) {
        fibArray[i] = fib(i-1) + fib(i-2)
    }
    return fibArray[i]
}

fib(fibSize)
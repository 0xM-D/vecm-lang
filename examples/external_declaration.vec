CLang {
        #include <stdio.h>

        const char * format = "Line %d\n";

        const char * getFormat() {
                return format;
        }
}

fn printf(s: *byte, ...) -> int;
fn getFormat() -> *byte;


export fn main() -> int {
        for (int i = 0; i < 10; i+=1) {
                printf(getFormat(), i);
        }
        return 0;
}